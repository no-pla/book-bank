/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["search1.kakaocdn.net", "user-images.githubusercontent.com"],
  },
};

module.exports = nextConfig;
